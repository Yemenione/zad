# الوثيقة الأكاديمية لمشروع التخرج: نظام "زاد" (Zaad) 🎓📜

هذه الوثيقة تقدم شرحاً تقنياً وهيكلياً مفصلاً لنظام "زاد" لإدارة الوجبات المدرسية، مُعدة خصيصاً لمناقشة التخرج في جامعة سبأ.

---

## 1. بنية النظام (System Architecture) 🏗️

يعتمد المشروع على معمارية **Decoupled Architecture** (انفصال الفرونتند عن الباكيند) لضمان السرعة والقابلية للتوسع:

- **الباكيند (Backend)**: تم بناؤه باستخدام إطار العمل **Laravel 12**.
  - **API Layer**: توفير البيانات لتطبيق الموبايل عبر Laravel Sanctum.
  - **Admin Panel**: لوحة تحكم متطورة باستخدام **FilamentPHP v3** للإدارة المركزية.
- **الفرونتند (Frontend)**: تطبيق موبايل تم بناؤه باستخدام **Flutter SDK**.
  - يتميز بالسرعة والأداء العالي (Native Performance).
- **قاعدة البيانات (Database)**: نظام **SQLite** (للتطوير المحلي) أو **MySQL** (للإنتاج).

---

## 2. تصميم قاعدة البيانات (Database Design) 🗄️

تتبع قاعدة البيانات نموذج **Relational Database Management System (RDBMS)** مع علاقات منطقية قوية:

### الجداول الرئيسية (Data Dictionary)

1. **جدول المستخدمين (Users)**:
    - يخزن بيانات أولياء الأمور، الإدارة، وعمال المقصف.
    - الحقول: `id`, `name`, `email`, `password`, `role`.
2. **جدول الأطفال (Children)**:
    - يربط كل طفل بولي أمره.
    - العلاقة: (Many-to-One) مع جدول المستخدمين.
3. **جدول السجل الصحي (Health Info)**:
    - يخزن مسببات الحساسية والمكونات الممنوعة لكل طفل.
    - العلاقة: (One-to-One) مع جدول الأطفال.
4. **جدول الوجبات (Meals)**:
    - قائمة الطعام المتاحة في المقصف مع مكوناتها وتفاصيلها.
    - الحقول: `name`, `price`, `ingredients` (JSON), `is_active`.
5. **جدول الطلبات (Orders)**:
    - يسجل عمليات الشراء، حالات الاستلام، ورموز الـ QR.
    - العلاقة: يربط `child_id` مع `meal_id`.

### مخطط العلاقات (ERD Logic)

- **ولي الأمر** يمكن أن يكون لديه **أكثر من طفل**.
- **الطفل** لديه **سجل صحي واحد** فقط.
- **الطلب** يربط **طفل محدد** بـ **وجبة محددة** في وقت معين.

---

## 3. الهيكل المجلداتي للمشروع (Project Structure) 📂

### مجلد الباكيند (`backend`)

- `app/Models`: نماذج البيانات وعلاقاتها (Eloquent Models).
- `app/Http/Controllers`: المتحكمات التي تعالج منطق الـ API.
- `app/Services`: يحتوي على "حارس الصحة" (HealthSafetyService).
- `database/migrations`: هيكل الجداول والقيود البرمجية.
- `app/Filament`: موارد لوحة التحكم الإدارية.

### مجلد الموبايل (`mobile`)

- `lib/pages`: واجهات التطبيق (Login, Menu, Profile).
- `lib/services`: التعامل مع الروابط (APIs) وجلب البيانات.
- `lib/models`: تمثيل البيانات القادمة من الباكيند داخل Flutter.

---

## 4. المنطق البرمجي الجوهري (Core Business Logic) 🧠

### حارس الصحة (Health Guard System)

هذا هو الابتكار الأساسي في المشروع. عند محاولة الطلب من التطبيق:

1. يقوم النظام بجلب مسببات الحساسية من جدول `health_info`.
2. يتم فحصها مقابل مكونات الوجبة في جدول `meals`.
3. إذا وجد تطابق، يتم إيقاف العملية فوراً وإرسال خطأ (403 Forbidden).

### نظام التوثيق بالـ QR

- يتم توليد رمز `qr_token` فريد لكل طلب مدفوع.
- يضمن هذا النظام أن الطفل يستلم الوجبة الصحيحة المخصصة له فقط.

---

**إعداد الطالبات:** مريم أكرم & إيمان فؤاد
**إشراف:** د. مهند المشرقي
**جامعة سبأ - كلية الحاسوب**
