# دليل تثبيت وتشغيل مشروع "زاد" (Zaad) 🛠️🎓

هذا الدليل مخصص للطالبتين **مريم أكرم** و**إيمان فؤاد** لشرح كيفية تشغيل النظام وتحويله إلى تطبيق هاتف جاهز للمناقشة.

---

## أولاً: إعداد بيئة العمل (الباكيند - Laravel)

### 1. المتطلبات

- تثبيت برنامج **XAMPP** (تأكد من تفعيل PHP 8.2 أو أحدث).
- تثبيت **Composer**.

### 2. خطوات التشغيل

1. افتح Terminal/CMD في مجلد `backend`.
2. نفذ الأمر: `composer install` (لتحميل المكتبات).
3. تأكد من وجود ملف `.env` (إذا لم يوجد، انسخ `.env.example` وقم بتسميته `.env`).
4. نفذ الأمر: `php artisan key:generate`.
5. **تهيئة قاعدة البيانات**:
   - تأكد من تشغيل Apache و MySQL من لوحة XAMPP.
   - نفذ الأمر: `php artisan migrate --seed --seeder=ZaadDemoSeeder`.
6. **بدء السيرفر**: `php artisan serve`.
   - سيظهر لك رابط مثل `http://127.0.0.1:8000`.

---

## ثانياً: إعداد وتشغيل تطبيق الموبايل (Flutter)

### 1. المتطلبات

- تثبيت **Flutter SDK**.
- تثبيت **Android Studio** (مع تحميل Android SDK ومحاكي Emulator).

### 2. خطوات التشغيل

1. افتح Terminal في مجلد `mobile`.
2. نفذ الأمر: `flutter pub get`.
3. لتشغيل التطبيق على المحاكي أو هاتفك الشخصي:
   - قم بتوصيل الهاتف وفك قفل "خيارات المطور".
   - نفذ الأمر: `flutter run`.

---

## ثالثاً: تحويل التطبيق إلى ملف APK (للهاتف) 📱

لجعل التطبيق يعمل على هاتفك بشكل دائم دون الحاجة للكمبيوتر:

1. افتح Terminal في مجلد `mobile`.
2. نفذ الأمر: `flutter build apk --release`.
3. ستجد الملف الناتج في المسار:
   `mobile/build/app/outputs/flutter-apk/app-release.apk`.
4. انقل هذا الملف لهاتفك وقم بتبيته.

---

## رابعاً: تعديل البيانات (التخصيص) ✍️

- **تغيير أسماء الطلاب والمشرف**: قمنا بتعديلها بالفعل في ملفات `composer.json` و `pubspec.yaml` ورؤوس الملفات (Headers).
- **تعديل الوجبات أو المستخدمين**: يمكنك ذلك بسهولة عبر لوحة التحكم:
  - ادخل للرابط: `http://127.0.0.1:8000/admin`.
  - استخدم حساب المدير: `admin@zaad.com` بكلمة مرور `admin123`.

---

## خامساً: الرفع إلى Git (التوثيق النهائي) ☁️

لقد قمت بالفعل ببرمجة أوامر Git لك. لرفع أي تغييرات جديدة مستقبلاً:

1. `git add .`
2. `git commit -m "تحديث نهائي للمشروع"`
3. `git push origin main` (إذا كان لديك مستودع على GitHub).

---
**إعداد:** مريم أكرم & إيمان فؤاد
**جامعة سبأ - 2026**
